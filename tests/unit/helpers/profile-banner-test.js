import {
  profileBanner
} from '../../../helpers/profile-banner';
import { module, test } from 'qunit';

module('ProfileBannerHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = profileBanner(42);
  assert.ok(result);
});
