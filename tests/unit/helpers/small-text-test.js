import {
  smallText
} from '../../../helpers/small-text';
import { module, test } from 'qunit';

module('SmallTextHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = smallText(42);
  assert.ok(result);
});
