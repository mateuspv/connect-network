import postFilter from '../../../utils/post-filter';
import { module, test } from 'qunit';

module('Unit | Utility | post filter');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = postFilter();
  assert.ok(result);
});
