import findWithQuery from '../../../utils/find-with-query';
import { module, test } from 'qunit';

module('findWithQuery');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = findWithQuery();
  assert.ok(result);
});
