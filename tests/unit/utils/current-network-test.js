import currentNetwork from '../../../utils/current-network';
import { module, test } from 'qunit';

module('currentNetwork');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = currentNetwork();
  assert.ok(result);
});
